# requirements
- Postgresql latest
- Nodejs latest
- Redis >= v6.x
- wget command

# install
- npm install
- cd src; cp .env.example .env
- update .env

# run
- npm run build
- cd build; node index.js
